<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title </title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

</head>
<body>



    @Html.Partial("_NavigationBar")

    @*This is for the Left Column*@

    

      @* -the classes-visible and hidden (AKA "Responsive Utility Classes)-"

                 controls how content responds to devices of differnt sizes

                 utility classes-visible and hidden

              - targets specific catagories of devices.
                 "hidden-xs"/"visible-xs"
                "hidden-sm"/"visible-sm"
                "hidden-md"/"visible-md"
                "hidden-lg"/"visible-lg"
        -This will hide/show any content, when on those certian device sizes-

        different column sizes to apply
              col-xs-(1-12)
              col-sm-(1-12)
              col-md-(1-12)
              col-lg-(1-12)
                 
          -tells bootstrap when to start stacking columns.-
                *@
              


    @* *body-content* is a class automatically generated by defult and put into code. Its not apart of the bootstrap code. *@
    <div class="container @*body-content*@">
        @* -*RenderBody* Displays Content On the Index Page- *@
        @RenderBody()
        <hr />
        @* by changing navbar to 
            navbar-pills: it will just round the corners
                          by adding stacked it will change the buttons on navbar from
                          being horizontal to vertical *@

        @* breadcrumb: will change the navbar to look as the following
                         home/contacts/about *@

                @* pagination: will put a border/box around each word
                            Add .pagination-lg or .pagination-sm for additional sizes. *@

        <div class="navbar navbar-inverse navbar-left">
            <div class="container">

                              @*Add .navbar-fixed-top and include a .container or .container-fluid to center and pad navbar content.
            example

            <nav class="navbar navbar-default navbar-fixed-top">
                <div class="container">
                    ...
                </div>
            </nav>
        Modify the look of the navbar by adding .navbar-inverse.

            </div>*@
                <div class="navbar-collapse collapse ">
                    <ul class="nav navbar-nav">
                        <li>@Html.ActionLink("Home", "Index", "HomePage")</li>
                        <li>@Html.ActionLink("About", "About", "HomePage")</li>
                        <li>@Html.ActionLink("Services", "Services", "HomePage")</li>
                        <li>@Html.ActionLink("Contacts", "ContactEmail", "HomePage")</li>
                        <li>@Html.ActionLink("Employment", "Employment", "HomePage")</li>                 
                        
                    </ul>
                   
                </div>

            </div>
        </div>
      
    </div>



    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    
    @RenderSection("scripts", required: false)

    @*<footer>
            <p>&copy; @DateTime.Now.Year - TheNorthernHandyman </p>
        </footer>*@
</body>


</html>


 